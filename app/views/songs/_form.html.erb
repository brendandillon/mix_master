<%= form_for([@artist, @song]) do |f| %>

  <% if @song.errors.any? %>
    <ul>
      <% @song.errors.full_messages.each do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  <% end %>
    
  <%= f.label :title%>
  <%= f.text_field :title %>

  <%= f.submit %>
<% end %>
